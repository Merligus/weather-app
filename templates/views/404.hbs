<!DOCTYPE html>

<html>
    <head>
        <title>{{title}}</title>
        <link rel="icon" href="./img/icon.png">
        <link rel="stylesheet" href="./css/styles.css">
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <h1>404</h1>
            <p>{{message}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>